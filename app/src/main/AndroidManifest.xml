<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Cinepople">

        <activity
            android:name=".activity.main.MainActivity"
            android:exported="true"
            android:noHistory="false">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.movie.MovieDetailsActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".activity.auth.LoginActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".activity.auth.ForgotActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".activity.auth.SignUpActivity"
            android:exported="false">
        </activity>

        <activity android:name=".activity.booking.SelectSessionActivity"
            android:exported="false">
        </activity>

        <activity android:name=".activity.booking.SelectSeatsActivity"
            android:exported="false">
        </activity>

        <activity android:name=".activity.booking.ReviewSummaryActivity"
            android:exported="false">
        </activity>

        <activity android:name=".activity.booking.PaymentActivity"
            android:exported="false" />

<!--         Facebook Login SDK -->
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:exported="false" />

        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>


        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />

        <meta-data
            android:name="com.google.android.gms.auth.api.credentials.ClientId"
            android:value="@string/default_web_client_id" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>